<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.classqa.data.management.ManageMapper">
    <insert id="insertUser" parameterType="com.example.classqa.po.User"
            useGeneratedKeys="true" keyProperty="id">
        insert into user(username, password)
        value(#{username},#{password})
    </insert>

    <delete id="deleteUser" >
        delete from user where username=#{username}
    </delete>

    <select id="selectUserByUsername" resultMap="User">
        select * from user where username=#{username};
    </select>

    <select id="selectUserByID" resultMap="User">
        select * from user where id=#{id};
    </select>

    <select id="selectAllUser" resultMap="User">
        select * from user
    </select>

    <update id="updateUserInfo">
        update user set username=#{username},password=#{password} where id=#{id}
    </update>

    <resultMap id="User" type="com.example.classqa.po.User">
    </resultMap>

</mapper>